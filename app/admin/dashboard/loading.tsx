import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Loading admin dashboard"
      description="Preparing operational shortcuts and current platform state."
    />
  );
}
