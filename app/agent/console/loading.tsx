import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Loading agent console"
      description="Preparing alias-safe ticket context, messages, and live-session state."
    />
  );
}
