import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Loading settings"
      description="Getting your workspace settings and access controls ready."
    />
  );
}